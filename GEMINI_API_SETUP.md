# วิธีขอ Gemini API Key (ฟรี) + ตั้งค่าหลายคีย์เพื่อกระจายโหลด

## 1) วิธีสมัคร/สร้าง API Key

1. เข้า **Google AI Studio**: https://aistudio.google.com/apikey
2. ล็อกอินด้วย Google Account (ใช้คนละบัญชี Gmail ต่อ 1 คีย์ ถ้าอยากได้โควต้าฟรีแยกกันจริงๆ)
3. กด **"Create API key"** → เลือกโปรเจกต์ (หรือสร้างโปรเจกต์ใหม่ก็ได้)
4. copy คีย์ที่ได้ (รูปแบบ `AIza...`) เก็บไว้ให้ปลอดภัย **ห้าม commit ลง git**
5. ทำซ้ำข้อ 2-4 ด้วย Google Account อื่นอีก 2 บัญชี เพื่อให้ได้ 3 คีย์ (โควต้าฟรีของ Gemini นับแยกตาม "โปรเจกต์/บัญชี" ไม่ใช่ตาม IP ของเรา)

> ⚠️ โควต้าฟรีของแต่ละคีย์แยกกันจริง แต่ถ้าสร้างทั้ง 3 คีย์จากโปรเจกต์ Google Cloud เดียวกัน บางกรณีโควต้าจะถูกนับรวมในระดับโปรเจกต์ ถ้าจะให้ได้ 3,000 ครั้ง/วันจริง แนะนำให้แยกคนละ Google account/โปรเจกต์

## 2) ตั้งค่าใน Render (Environment Variables)

ไปที่ Render Dashboard → service ของเกม → **Environment** → เพิ่มตัวแปรนี้ (เลือกวิธีใดวิธีหนึ่ง):

**แบบที่แนะนำ — คั่นด้วยจุลภาคตัวแปรเดียว:**
```
GEMINI_API_KEYS=AIzaXXXXXXXXXXXX1,AIzaXXXXXXXXXXXX2,AIzaXXXXXXXXXXXX3
```

**หรือแยกเป็นตัวแปรทีละคีย์ (เผื่ออยากดูใน dashboard แยกกัน):**
```
GEMINI_API_KEY_1=AIzaXXXXXXXXXXXX1
GEMINI_API_KEY_2=AIzaXXXXXXXXXXXX2
GEMINI_API_KEY_3=AIzaXXXXXXXXXXXX3
```

ยังใช้ `GEMINI_API_KEY` ตัวเดียวแบบเดิมได้เหมือนกัน (backward compatible) — ถ้าตั้งไว้หลายแบบพร้อมกัน โค้ดจะรวมทุกคีย์ที่เจอ (ตัดตัวซ้ำออก) แล้วใช้ทั้งหมด

รันเสร็จลอง `GET /health` จะเห็น `gemini_keys_configured` / `gemini_clients_ready` บอกจำนวนคีย์ที่โหลดสำเร็จ

## 3) วิธีกระจายโหลดที่ใช้ — สุ่มคีย์ + fallback อัตโนมัติ

โค้ดใน `app.py` (ฟังก์ชัน `_generate_with_failover`) ทำงานแบบนี้ทุกครั้งที่มีคนถ่ายรูป:

1. สุ่มลำดับคีย์ทั้ง 3 ใหม่ทุกรีเควสต์
2. ยิง request ด้วยคีย์แรกในลำดับ
3. ถ้าคีย์นั้นโดน **429 (โควต้าเต็ม)** หรือ **503 (Gemini overload)** → ข้ามไปคีย์ถัดไปทันที ไม่ error กลับไปที่ผู้เล่น
4. ถ้าทุกคีย์ล้มเหมือนกันหมด (โควต้าฟรีทั้ง 3 คีย์เต็มพร้อมกัน) → ถึงจะ error จริง

### ทำไมเลือก "สุ่ม + fallback" แทน "round-robin (1,2,3 ไล่ตามลำดับ)"

เกมนี้ deploy บน Render ผ่าน `gunicorn` ซึ่งรันได้หลาย **process** พร้อมกัน (แต่ละ process มีหน่วยความจำแยกกัน) ถ้าจะทำ round-robin จริง (ไล่ 1→2→3→1→2→3...) ต้องมีตัวนับที่ share กันข้าม process เช่นเก็บไว้ใน Redis/DB — ซึ่งโปรเจกต์นี้ไม่มี infra แบบนั้น ถ้าใส่ตัวนับแบบ global variable ในโค้ด Python ธรรมดา แต่ละ process จะนับแยกกันเอง กลายเป็น "สุ่มโดยไม่ได้ตั้งใจ" อยู่ดี แต่มี state ที่ดูแลยากกว่าโดยไม่ได้ประโยชน์เพิ่ม

**สุ่ม (random) ให้ผลลัพธ์เดียวกันโดยไม่ต้องมี state เลย** — ไม่ต้องพึ่ง Redis, ทำงานถูกต้องแม้มีหลาย process/worker, และเมื่อรันจำนวนครั้งมากพอ (เช่น เกมที่มีคนเล่นเป็นสิบ/ร้อยครั้งต่อวัน) การกระจายจะเฉลี่ยเข้าใกล้ 33/33/33% เอง

ส่วนที่สำคัญกว่าความสม่ำเสมอ 100% คือ **fallback อัตโนมัติเมื่อคีย์ใดคีย์หนึ่งโควต้าหมด** — ซึ่งวิธี round-robin เพียวๆ ไม่มีให้ ถ้าไม่เพิ่ม logic แยก แต่การสุ่ม+fallback ที่ทำไว้ให้แล้วคือจุดที่ทำให้เกมไม่ล่มกลางวันแม้คีย์ใดคีย์หนึ่งจะโควต้าเต็มก่อนก็ตาม

**สรุป:** ใช้ 3 คีย์ (ฟรี 1,000 ครั้ง/คีย์/วัน) รวมได้สูงสุด ~3,000 ครั้ง/วัน โดยระบบสุ่มเลือกคีย์ + ข้ามไปคีย์ที่ยังมีโควต้าให้อัตโนมัติ ไม่ต้องทำอะไรเพิ่มเวลาใช้งานจริง
